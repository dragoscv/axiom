{
  "name": "axiom-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter ./packages... run build",
    "dev": "pnpm -r --filter ./packages... run dev",
    "test": "pnpm -r --filter @axiom/tests run test",
    "lint": "pnpm -r --filter ./packages... run lint || echo 'no lints yet'"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "zod": "^3.23.8",
    "tsx": "^4.19.0"
  }
}